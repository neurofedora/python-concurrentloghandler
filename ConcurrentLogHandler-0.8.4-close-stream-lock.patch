diff -ur ConcurrentLogHandler-0.8.4.orig/src/cloghandler.py ConcurrentLogHandler-0.8.4/src/cloghandler.py
--- ConcurrentLogHandler-0.8.4.orig/src/cloghandler.py	2013-05-16 17:07:52.243946629 +1000
+++ ConcurrentLogHandler-0.8.4/src/cloghandler.py	2013-05-16 17:07:40.564701852 +1000
@@ -209,6 +209,8 @@
         if not self.stream.closed:
             self.stream.flush()
             self.stream.close()
+        if not self.stream_lock.closed:
+            self.stream_lock.close()
         Handler.close(self)
     
     def flush(self):
